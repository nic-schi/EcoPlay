<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Base application theme. -->
    <style name="Theme.EcoPlay" parent="Theme.AppCompat.Light">
        <!-- ActionBar -->
        <item name="android:actionBarStyle">@style/Theme.EcoPlay.ActionBar</item>
        <item name="actionBarStyle">@style/Theme.EcoPlay.ActionBar</item>

        <item name="android:windowBackground">@color/background</item>
        <item name="android:colorBackground">@color/background</item>

        <item name="android:textColor">@color/text_color</item>

        <item name="android:lineSpacingExtra">8dp</item>

        <item name="android:actionOverflowButtonStyle">@style/OwnActionButtonOverflow</item>
        <item name="actionOverflowButtonStyle">@style/OwnActionButtonOverflow</item>

        <item name="android:overlapAnchor">false</item>
        <item name="overlapAnchor">false</item>
    </style>

    <!-- ActionBar -->
    <style name="Theme.EcoPlay.ActionBar" parent="@style/ThemeOverlay.AppCompat.Dark.ActionBar">
        <item name="titleTextStyle">@style/TextAppearance.Apptitel</item>
        <item name="android:titleTextStyle">@style/TextAppearance.Apptitel</item>

        <item name="android:background">@drawable/action_bar_bg</item>
        <item name="background">@drawable/action_bar_bg</item>

        <item name="android:logo">@drawable/logo</item>
        <item name="logo">@drawable/logo</item>

        <item name="fontFamily">@font/roboto_bold</item>

        <item name="android:height">@dimen/action_bar.height</item>
        <item name="height">@dimen/action_bar.height</item>

        <item name="android:homeAsUpIndicator">@drawable/go_back_btn</item>
        <item name="homeAsUpIndicator">@drawable/go_back_btn</item>

        <item name="android:elevation">10dp</item>

        <item name="android:windowActionBarOverlay">true</item>
        <item name="windowActionBarOverlay">true</item>

        <item name="android:displayOptions">showHome|useLogo|showTitle</item>
        <item name="displayOptions">showHome|useLogo|showTitle</item>

        <item name="actionBarWidgetTheme">@style/Theme.Ecoplay.ActionBar.Widget</item>
    </style>

    <style name="Theme.Ecoplay.ActionBar.Widget" parent="@style/Theme.AppCompat">
        <item name="android:paddingLeft">32dp</item>
        <item name="android:paddingRight">32dp</item>
    </style>

    <!-- Overflow button -->
    <style name="OwnActionButtonOverflow" parent="@style/Widget.AppCompat.ActionButton.Overflow">
        <item name="android:src">@drawable/actionbar_overflow</item>
    </style>

    <!-- Buttons -->
    <style name="Button.EcoPlay" parent="@style/Widget.AppCompat.Button.Colored">
        <item name="android:paddingStart">20dp</item>
        <item name="android:paddingEnd">20dp</item>
        <item name="android:paddingBottom">15dp</item>
        <item name="android:paddingTop">15dp</item>

        <item name="android:backgroundTint">@color/akzent</item>
        <item name="backgroundTint">@color/akzent</item>

        <item name="fontFamily">@font/roboto_medium</item>
        <item name="android:textAllCaps">false</item>
        <item name="android:textSize">17sp</item>
        <item name="elevation">10dp</item>
        <item name="android:elevation">10dp</item>
    </style>

    <!-- Imagebutton -->
    <style name="ImageButton.EcoPlay" parent="Widget.AppCompat.ImageButton">
        <item name="android:paddingStart">10dp</item>
        <item name="android:paddingEnd">10dp</item>
        <item name="android:paddingBottom">10dp</item>
        <item name="android:paddingTop">10dp</item>

        <item name="android:backgroundTint">@color/akzent</item>
        <item name="backgroundTint">@color/akzent</item>

        <item name="elevation">10dp</item>
        <item name="android:elevation">10dp</item>
    </style>

    <!--- TextAppearances -->
    <!-- Standard -->
    <style name="TextAppearance.EcoPlay" parent="TextAppearance.AppCompat.Body1">
        <item name="android:textColor">@color/text_color</item>
        <item name="android:textSize">16sp</item>
        <item name="fontFamily">@font/roboto_regular</item>
        <item name="android:fontFamily">@font/roboto_regular</item>
    </style>

    <!-- Header -->
    <style name="TextAppearance.EcoPlay.Header" parent="TextAppearance.EcoPlay">
        <item name="android:textSize">24sp</item>
        <item name="fontFamily">@font/roboto_medium</item>
        <item name="android:fontFamily">@font/roboto_medium</item>
    </style>

    <!-- AppTitel -->
    <style name="TextAppearance.Apptitel" parent="TextAppearance.EcoPlay">
        <item name="android:textSize">@dimen/apptitel.text_size</item>
        <item name="android:textColor">@color/apptitel</item>

        <item name="fontFamily">@font/roboto_bold</item>
        <item name="android:fontFamily">@font/roboto_bold</item>
    </style>

    <!-- Onboarding AppTitel -->
    <style name="TextAppearance.Onboarding.AppTitel" parent="TextAppearance.Apptitel">
        <item name="android:textSize">@dimen/onboarding.apptitel.text_size</item>
    </style>
</resources>